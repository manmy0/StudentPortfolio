@{
    @if (!this.User.Identity.IsAuthenticated || User.IsInRole("Admin") || User.IsInRole("Staff"))
    {
          Layout = "_Layout";
    }
    else if (User.IsInRole("Student"))
    {
        Layout = "_LayoutUser";
    }

}
