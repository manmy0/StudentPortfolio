@{
    @if (!this.User.Identity.IsAuthenticated || User.IsInRole("Admin"))
    {
          Layout = "_Layout";
    }
    else if (User.IsInRole("Student") || this.User.Identity.IsAuthenticated)
    {
        Layout = "_LayoutUser";
    }

}
